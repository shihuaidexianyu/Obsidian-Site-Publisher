# Fixtures

These fixture vaults exist to keep parser, diagnostics, staging, and builder behavior stable as implementation grows.

- Each directory should model one focused behavior.
- New bug fixes should add a minimal reproducer here.
- Fixtures should prefer small markdown examples over large synthetic vaults.
